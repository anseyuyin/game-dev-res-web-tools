# 统一换行符为 LF，避免跨平台 diff 噪声
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
*.ttf binary
*.woff binary
*.woff2 binary

# 文档在 Windows 上以 CRLF 检出更友好（可选，如不需要可删除下面几行）
*.md text eol=crlf
